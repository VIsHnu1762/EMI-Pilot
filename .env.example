# Environment Configuration for EMI-Pilot

# MongoDB Connection
# For local MongoDB:
MONGODB_URI=mongodb://localhost:27017/emi-pilot

# For MongoDB Atlas (replace with your credentials):
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/emi-pilot

# API URL for frontend to connect to backend
NEXT_PUBLIC_API_URL=http://localhost:4000/api

# Server Port (optional, defaults to 4000)
PORT=4000
